const MOD = Math.pow(10, 9) + 7;
/**
 * @param {number} n
 * @param {number} k
 * @param {number} target
 * @return {number}
 */
var numRollsToTarget = function (n, k, target) {
  const values = new Array(k).fill().map((v, i) => i + 1);
  return 0;
};

function getSet(n, k, i, sum, target) {
    if(i > n){
        return;
    }
    if(sum < 0){
        return;
    }
    if(sum === 0){
        
    }
    for(let )

}
